  <!-- Début Nav -->
  <div class="d-flex start-end">
    <div class="col-3"></div>
    <nav class="d-flex justify-content-center col-6">
      <ul class="list-unstyled d-flex">
          <li><a href="{{ path('app_intervention', {'show':'photos','idInter': intervention.id, 'action':'show', 'addCategorieTache':'null'})}}" class="text-black text-decoration-none fw-semibold p-2 nav-liens rounded-pill">Photos</a></li>
          <li><a href="{{ path('app_intervention', {'show':'taches','idInter': intervention.id, 'action':'show', 'addCategorieTache':'null'})}}" class="text-white text-decoration-none fw-semibold p-2 nav-liens rounded-pill">Tâches</a></li>
      </ul>
    </nav>
  </div>
  <!-- Fin Nav -->

  <!-- Début Taches -->
  <div class="grap d-flex  overflow-auto">
    <!-- Début section catégories existante-->
    {% for categorieTache in categorieTaches %}
    <div class="p-2 text-center col-11 col-md-4 mx-3">
      <span class="roboto-medium">{{ categorieTache.nomCat }} </span> 
      <div class="bg-white rounded-2 mt-1 p-3" style="height:490px">
        <!-- Début A travailler pour la suppression d'une tache -->
        <div class="text-center my-2 py-2 taches rounded-2">
          <a href="{{path('supprimer_tache', {'idInter': intervention.id, 'idTache': 1})}}"><span class="kalam-regular">Suppimer tache</span></a>
        </div>
        {# {% for tache in categorieTache.taches %}
        <div class="text-center my-2 py-2 taches rounded-2">
          <span class="kalam-regular"{{ tache.description}}></span>
        </div>
        {% endfor %} #}
        <!-- Fin A travailler pour la suppression d'une tache -->

        <!-- Début section ajout d'une tâche -->
        {% if (action == 'ajoutTache' and addCategorieTache == categorieTache.nomCat) %}
        <!-- Début Section formulaire caché -->
          <div class="text-center my-2 py-2 taches rounded-2">
            {{ form_start(ajoutTacheForm) }}
            {{ form_row(ajoutTacheForm.description) }}
            <button type="submit"> ajouter </button>
            <input name="idCat" type="hidden" value="categorie.id">
            {{ form_end(ajoutTacheForm) }}
          </div>
          {% else %}
            <div class="text-center my-2 py-2 taches rounded-2">
          <a href="{{path('app_intervention', {'show':'taches','idInter':intervention.id, 'action':'ajoutTache', 'addCategorieTache':categorieTache.nomCat})}}"> <i class="fa-solid fa-plus"></i></a>
        </div>

        {% endif %}
        <!-- Fin Section formulaire caché -->
        
        <!-- Fin section ajout d'une tâche -->

      </div>
    </div>
    {% endfor %}
    <!-- Fin section catégories existante-->

    <!-- Début formulaire ajout catégorie-->
    <div class="p-2 text-center col-11 col-md-4 mx-3">
      {{ form_start(ajoutCategorieForm) }}
      <span class="roboto-medium">Nouvelle catégorie</span>
      <div class="bg-white bg-opacity-50 border border-2 border-white rounded-2 d-flex align-items-center justify-content-center mt-1" style="height:490px">
        <div class="d-flex flex-column text-center py-2 ">
          {{ form_row(ajoutCategorieForm.nomCat) }}
          <button type="submit" class="btn btn-primary ">Créer</button>
        </div>
      </div>
      {{ form_end(ajoutCategorieForm) }}
    </div>
    <!-- Fin formulaire ajout catégorie-->
  </div>
  <!-- Fin Taches  -->