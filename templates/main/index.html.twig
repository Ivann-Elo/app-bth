{% extends 'base.html.twig' %}

{% block title %}Tableau de bord{% endblock %}

{% block body %} 
    <main class="container-fluid">
        <div class="row">
            <!-- Début de la colonne de gauche -->
            {% include "partials/sidebar/sidebar.html.twig" %}
            <!-- Fin de la colonne de gauche -->
            
            <!-- Début de la colonne de droite -->
            <div class="col-lg-9 py-3 offset-lg-3">
                <h1 class="text-center">{{ titrePage }}</h1>
                <span id='showInterventions' class="roboto-medium active border-bottom border-success pb-1">Mes interventions</span>&nbsp;&nbsp;
                <span id='showClients' class="roboto-medium text-secondary">Mes clients</span>
                    <div class="mt-3" id="showInterDiv">
                        {% include "partials/liste_inter.html.twig" %} 
                    </div>
                    <div  class="d-none mt-3" id="showClientDiv">
                        {% include "partials/liste_client.html.twig" %}
                    </div>
            </div>
            <!-- Fin de la colonne de droite -->
        </div>
    </main>    
{% endblock %}
